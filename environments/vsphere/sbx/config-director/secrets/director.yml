---
# I added the certificate from director credhub -n /services/tls_ca - so that CloudCache could work ... also need to add this to TAS (Certificate Authorities Trusted by Router and HAProxy)
trusted_certificates: ((director_trusted_cert.certificate))
#trusted_certificates:
vcenter_host: ((vcenter_hostname))
vcenter_password: ((vcenter_password))
vcenter_username: ((vcenter_userid))

nsx_ca_certificate: ((nsx_ca_certificate.certificate))
nsx_password: ((nsx_auth_simple_nsx_api_password))

test_no_credhub_value: ((test-no-credhub-value))