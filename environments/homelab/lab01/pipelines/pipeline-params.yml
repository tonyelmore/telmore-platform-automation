pipeline_params:
  iaas: homelab
  foundation: lab01
  repo:
    uri: h’}
    branch: master
    # public_key: ((github_public_key))
    # Need private key

pivnet-product:
  slug: elastic-runtime
  version: 6.0.15

s3_product:
  bucket: products
  region_name: us-west-2
  s3_endpoint: minio.lab.telmore.io
  regexp: 

concourse:
  team:
    product_s3_filename:
      access_key_id:
      bucket:
      endpoint:
      # Need secret access key
      skip_ssl_verification: false
      foundation_artifacts:
        bucket: foundation_runtime
        